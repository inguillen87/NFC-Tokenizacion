# nexID Tokenization / Polygon Amoy
# disabled = fail closed; no simulation or transaction.
# simulated = explicit demo/sandbox state, without tx hash or token id.
# polygon = real Amoy tx required; no simulated anchor fallback.
#
# Before enabling polygon, run:
# npm run tokenization:check
TOKENIZATION_MODE=disabled
# Global gate only. A batch/tenant must also explicitly persist policy plus
# auto_tokenize_on_valid_tap=true; vertical inference never authorizes a mint.
SUN_AUTO_TOKENIZE_ON_VALID_TAP=false
TOKENIZATION_UID_SALT=replace-with-at-least-32-random-bytes
TOKENIZATION_METADATA_CID_PREFIX=nexid-metadata

# Option A: Polygon Amoy local minter runtime. Backend/API only. Never use NEXT_PUBLIC_*.
# TOKENIZATION_USE_LOCAL_MINTER=true
POLYGON_RPC_URL=https://polygon-amoy.g.alchemy.com/v2/your-key
POLYGON_MINTER_PRIVATE_KEY=0x...
POLYGON_MINTER_ADDRESS=0x...
POLYGON_CONTRACT_ADDRESS=0x...
POLYGON_DEPLOY_OWNER=0x...

# Option B: premium executor/KMS-style boundary. Use instead of POLYGON_MINTER_PRIVATE_KEY in API.
# The executor service keeps RPC, signer and contract custody. The recipient
# comes only from a verified consumer wallet or explicit tenant/batch config.
# TOKENIZATION_USE_LOCAL_MINTER=false
# TOKENIZATION_EXECUTOR_URL=https://executor.nexid.lat/mint
# TOKENIZATION_EXECUTOR_SECRET=replace-with-at-least-32-random-bytes
# INTERNAL_TOKENIZATION_KEY=replace-with-at-least-32-random-bytes
